[project]
name = "polenet-rebuild"
version = "1.0.0"
description = "Static-site generator for the polenet.org rebuild"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
]

[tool.uv]
package = false
